---
 - name: executing script file on master
   hosts: localhost
   connection: local
   become: true
   tasks:
     - name: calling master file
       script: master.sh

 - name: executing script file on agent
   hosts: app
   become: true
   tasks:
     - name: calling agent file
       script: agent.sh
